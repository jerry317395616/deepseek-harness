# Opt-in Tongjianyun nutrition-rule Bundle. The row stays disabled until a
# deployment explicitly enables the local Native Bench adapter (or chooses the
# authenticated MCP compatibility mode) through a later profile patch.

- insert:
    - id: tongjianyun-nutrition-rules
      name: '@deepseek-ai/dsh-tool-tongjianyun-nutrition-rules'
      disabled: true
